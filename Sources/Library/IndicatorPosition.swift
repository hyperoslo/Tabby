import Foundation

/// `IndicatorPosition` is used in `TabbyBar` to configure where the indicator should be constrained.
///
/// - top: Constrainted to the top of the tab bar.
/// - bottom: Constrainted to the bottom of the tab bar.
public enum IndicatorPosition {
  case top, bottom
}
